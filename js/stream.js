document.addEventListener("DOMContentLoaded", function() {
    let video = document.getElementById("live-video");
    let preImage = document.getElementById("pre-image");
    let postImage = document.getElementById("post-image");
    let liveButton = document.getElementById("live-button");

    // Cuando el usuario le da play al video
    video.addEvent
